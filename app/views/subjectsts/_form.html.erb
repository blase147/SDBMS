<%= form_with(model: subjectst) do |form| %>
  <% if subjectst.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subjectst.errors.count, "error") %> prohibited this subjectst from being saved:</h2>

      <ul>
        <% subjectst.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
