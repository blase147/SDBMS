<%= form_with(model: student) do |form| %>
  <% if student.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
        <% student.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="grid_container">  

    <!-- Concatenate firstname and lastname as fullname -->
    <div class="grid_item">
      <%= form.label :fullname , style: "display: block" %>
      <%= form.collection_select :admission_id, @students_collection, :id, :fullname %>
    </div>

    <div class="grid_item">
      <%= form.label :classroom, style: "display: block" %>
      <%= form.collection_select :classroom_id, @classrooms, :id, :name %>
    </div>

    <div class="grid_item">
      <%= form.label :admission_number, style: "display: block" %>
      <%= form.text_field :admission_number %>
    </div>

    <div class="grid_item">
      <%= form.label :registration_date, style: "display: block" %>
      <%= form.date_field :registration_date %>
    </div>

    <div >
      <%= form.label :disabilities %>
      <%= form.check_box :disabilities %>
    </div>

    <div class="grid_item">
      <%= form.label :disability_type, style: "display: block" %>
      <%= form.text_field :disability_type %>
    </div>

    <div>
      <%= form.submit %>
    </div>
  </div>
<% end %>

