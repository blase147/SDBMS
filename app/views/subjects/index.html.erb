<p style="color: green"><%= notice %></p>

<h1>Subjects</h1>

<div id="subjectsts">
  <% @subjects.each do |subject| %>
    <%= render subject %>
    <p>
      <%= link_to "Show this subject", subject %>
    </p>
  <% end %>
</div>

<%= link_to "New subjectst", new_subjectst_path %>
